export interface OpenSourceWork {
  name: string
  description: string
  url: string
}
export interface OpenSourceContent {
  title: string
  description: string
  sideNote: string
  works: OpenSourceProject[]
}
